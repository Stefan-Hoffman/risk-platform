spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/risk_platform_test
    username: postgres
    password: Swarttoon111
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    clean-disabled: false
    locations: classpath:db/migration